# Database Configuration
# Local defaults for loopback access. CI/production env overrides these via DB_ADMIN_HOST/DB_ADMIN_PORT/DB_NAME/DB_USER/DB_PASSWORD
db.host=127.0.0.1
db.port=15433
db.name=card_cloud
db.user=card_cloud
db.password=
